[project]
name = "university-management-system"
version = "0.1.0"
description = "University Management System Flask App"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "Flask>=3.0.0",
]